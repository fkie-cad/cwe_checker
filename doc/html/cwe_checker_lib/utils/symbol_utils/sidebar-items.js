initSidebarItems({"fn":[["find_symbol","Find the extern symbol object for a symbol name and return the symbol tid and name."],["get_calls_to_symbols","Match direct calls’ target tids in the program’s subroutines with the tids of the external symbols given to the function. When a match was found, add a triple of (caller name, callsite tid, callee name) to a vector. Lastly, return the vector with all callsites of all given external symbols."],["get_callsites","Find calls to TIDs contained as keys in the given symbol map. For each match return the block containing the call, the jump term representing the call itself and the symbol corresponding to the TID from the symbol map."],["get_symbol_map","Get a map from TIDs to the corresponding extern symbol struct. Only symbols with names contained in `symbols_to_find` are contained in the map."]]});