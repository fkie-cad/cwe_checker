(library
  (name cwe_checker)
  (libraries
  alcotest
  yojson
  bap
  core_kernel)
  (preprocess (pps ppx_jane))
  (wrapped false) ; needed so that all submodules are accessible for library users
)

(include_subdirs unqualified) ; Include all subdirs when looking for source files
