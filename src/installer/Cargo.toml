[package]
name = "cwe_checker_install"
version = "0.1.0"
edition = "2021"

[dependencies]
directories = "4.0.1"
walkdir = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0" # for easy error types
